// Checkpoint 2 stub — real Playwright + ffmpeg pipeline lands later.
console.log("[demobro-worker] idle stub running");

setInterval(() => {
  console.log("[demobro-worker] heartbeat — waiting for jobs");
}, 60_000);
